Check out `lib/stdlib.ss` for an idea of what's available when you load the REPL.

added:
	floating point numbers
	reload and exit procs

todo:
	symbol table should be a hash table
	time function (wip)
	garbage collection











